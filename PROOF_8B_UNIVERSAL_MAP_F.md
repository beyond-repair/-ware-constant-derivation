# Proof 8B — Derivation of the universal map F (and 8B-N)

**Date:** 2026-09-23. 5R–8A frozen. χ remains undefined until a non-arbitrary F exists. Forbidden inputs: 0.08, 0.23, W(n), a0, Ω_c, r_p, thrust.

## Question

Does the frozen corpus force a dimensionless map χ = F[observable] that is the same at every scale, with derived normalization?

## A. Spectral candidates from K(W)=ω²I-WL

Dimensionless objects that exist without a new axiom:

| Object | Status |
|---|---|
| x_k = W λ_k / ω² | Dimensionless. Contains external W. Not an output. |
| W_crit = ω²/λ_max | Bound, not a measured invariant of nature. Normalization of L is a model choice (λ_max=6 on the locked gasket). |
| λ_k/λ_max | Spectrum-only. Graph-dependent. Which k is FREE. |
| Tr(L^n)/(Tr L)^n | Spectrum-only. n is FREE. Divergent for continuum L without a cutoff. |
| q_th / ω² = 4 | DERIVED (6C.1). Internal two-particle threshold of this QFT. Same in every copy of the model. Does not map r_p to Ω_c. |

None of these is a map F from an independent observable to χ. The only fully derived dimensionless number in this class is 4, and it is the bubble threshold, not a universal coupling.

Coarse-graining: no derived map W(x)→W(n) (still P22). Spectral moments of the gasket do not become proton or halo observables without an identification of the gasket with those systems. That identification is ASSUMED / POST-HOC.

## B. RG invariants

An RG invariant needs a flow μ d g_i / dμ = β_i and a combination I(g,μ) with μ dI/dμ = 0.

The freeze has scheme remainders (6B MSbar piece ∝ 4/3-log(ω²/μ²)) and no β(W): S_W is missing (6D–7B). The MSbar remainder depends on μ, so it is not χ.

Status: RG-FIXED χ **not constructed**. UNDERDETERMINED pending S_W and a beta function.

## C. Fixed points

β(g*)=0 is not derived. No candidate g*. IMPOSSIBLE inside the freeze; not a no-go against all QFTs.

## D. Geometric / topological (connected repos)

sierpinski-geometry-045 locks 0.45 as a **design** scale factor (README: not a spectral-dimension threshold, not a force). Gasket identities 3/5 (energy), 5/3 (two-corner resistance), 1/5 (Dirichlet λ_min limit) are TOPOLOGICALLY FIXED for that PCF gasket.

They fail the 8A filter: there is no derived transformation that sends those gasket numbers to a proton radius or a halo. Using 3/5 or 0.45 as χ_p or χ_G is POST-HOC identification.

topological-pinch, stress-tensor-modification, momentum-closure are thrust-stack geometry. They do not define F. Thrust targets remain forbidden inputs.

## E. Ratio of two dynamically generated scales

Legitimate form: χ = L_emergent / L_fundamental when both are outputs of the same construction.

In the freeze:

- ω is an INPUT.
- {λ_k} is an INPUT (or a graph choice).
- The only derived scale from dynamics of the locked loop is q_th = 4ω², a mass-squared, ratio 4 to ω².

Two independent emergent lengths (e.g. proton vs Hubble) are not generated. E is empty except the internal 4.

## 8B-N — Scoped no-go

**Proved.** No non-arbitrary F that (i) is fixed before data, (ii) has derived normalization, (iii) maps two independent physical observables, can be built from 5R–8A alone.

Every candidate either contains external W, a FREE index/normalization, a μ-dependent remainder, a graph-bound constant without a scale map, or the internal number 4 which does not connect scales.

**Not proved.** No deeper theory can contain an F. That would be a different package.

## Decision tree (this package)

- Derive F that maps independent observables: NO
- Kill universal-χ as a claim of the freeze: YES (undefined and no F)
- Kill F as constructed here: YES (underdetermined / arbitrary if forced)
- Identify missing physics: S_W, beta function, or a derived micro↔macro map

8C (cross-scale numerical test) is **not licensed**. Running χ_p vs χ_G without F repeats 8A numerology.

```
experimental_validation     = false
thrust_validated            = false
energy_extraction_validated = false
```
